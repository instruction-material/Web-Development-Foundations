# Deployment Notes

- Front end and back end should be deployable separately.
- The public site can be static-hosted or served behind Nginx.
- The API needs environment variables for allowed origin and secrets.
- Confirm logs and health endpoints after deployment.

